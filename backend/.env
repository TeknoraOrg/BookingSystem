sh
DB_HOST=localhost
DB_USER=root
DB_PASS=password
DB_NAME=booking_system
PORT=5432
# Example .env configuration
REACT_APP_API_URL=http://localhost:5000/api  # If using mock data, this might not be needed